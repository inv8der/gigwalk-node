machine:
  node:
    version: 8.9.1

test:
  post:
    - yarn run flow
    - yarn run eslint

deployment:
  npm:
    branch: master
    owner: gigwalk-corp
    commands:
        - yarn run semantic-release || true
